rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Require authentication for rights; allow public reads for the moment feed.
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
