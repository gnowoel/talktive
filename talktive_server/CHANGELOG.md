## 1.1.0

- Fixed invalid FCM payload field (`priority` in `android.notification`)
- Trigger `sendMessageNotification` for private and group messages in `MessageEndpoint`
- Improved Plaza route resolution in `NotificationService`
- Added debug logging for FCM notification sends

## 1.0.0

- Initial version, created by Stagehand
