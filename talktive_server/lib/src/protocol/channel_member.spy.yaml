class: ChannelMember
table: channel_member
fields:
  channelId: int
  userInfoId: UuidValue
  joinedAt: DateTime
  role: String?
  status: ChannelMemberStatus
indexes:
  channel_user_idx:
    fields: channelId, userInfoId
    unique: true
