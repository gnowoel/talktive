class: Moment
table: moment
fields:
  authorId: int
  imageUrl: String
  caption: String?
  createdAt: DateTime
  likesCount: int
  commentsCount: int
  
  # Denormalized Author Info
  authorName: String
  authorAvatar: String
  authorFloor: int
