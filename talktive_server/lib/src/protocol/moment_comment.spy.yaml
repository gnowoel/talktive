class: MomentComment
table: moment_comments
fields:
  momentId: int
  userId: UuidValue
  text: String
  createdAt: DateTime

  # Denormalized
  userName: String
  userAvatar: String
  userMood: String?
  userFloor: int

indexes:
  moment_comment_created_idx:
    fields: momentId, createdAt
