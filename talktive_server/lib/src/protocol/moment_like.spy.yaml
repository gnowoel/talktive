class: MomentLike
table: moment_likes
fields:
  momentId: int
  userId: UuidValue
  createdAt: DateTime

  # Denormalized Cache
  userName: String
  userAvatar: String
  userMood: String?
  userFloor: int

indexes:
  moment_user_unique:
    fields: momentId, userId
    unique: true
