class: Report
table: report
fields:
  reporterId: UuidValue
  targetId: UuidValue
  reason: String
  channelId: int?
  messageId: int?
  createdAt: DateTime
  resolved: bool
indexes:
  report_target_idx:
    fields: targetId, createdAt
  report_reporter_idx:
    fields: reporterId, createdAt
