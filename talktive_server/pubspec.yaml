name: talktive_server
description: Starting point for a Serverpod server.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '^3.8.0'

dependencies:
  serverpod: 3.3.1
  serverpod_auth_idp_server: 3.3.1
  serverpod_auth_core_server: 3.3.1
  serverpod_auth_server: 3.3.1
  uuid: ^4.5.2
  firebase_admin: ^0.3.0
  googleapis_auth: ^1.6.0
  http: ^1.2.0
  image: ^4.3.0
  path: ^1.9.0
  dart_jsonwebtoken: ^3.3.2

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  serverpod_test: 3.3.1
  test: ^1.25.5

serverpod:
  scripts:
    # Starts the server and applies migrations
    start: dart bin/main.dart --apply-migrations

    # Build the Flutter web app and copy it to the server's web directory.
    # Windows uses xcopy since flutter's --output flag doesn't work there.
    # See: https://github.com/flutter/flutter/issues/157886
    flutter_build:
      windows: >-
        (if exist web\app rmdir /S /Q web\app)
        & cd /D ..\talktive_flutter
        && flutter build web --base-href /app/ --wasm
        && xcopy /E /I /Y build\web ..\talktive_server\web\app
      posix: |
        cd ../talktive_flutter
        flutter build web --base-href /app/ --wasm --output ../talktive_server/web/app
